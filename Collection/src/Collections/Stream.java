package Collections;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.function.Function;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class Stream {
	public static void main(String[] args) {
		
		List<String> strings = Arrays.asList("apple", "banana", "pear", "cherry");
 		
//		String str = "hello world";
//		str.toLowerCase().trim();
// 
//		Map<Character, Long> collect = str.chars().mapToObj(a->(char)a).collect(Collectors.groupingBy(Function.identity(),Collectors.counting()));
//		collect.forEach((a,b)->System.out.println(a+" : "+b));
//		List<Integer> list = Arrays.asList(1,2,3,4);
		
//		int sum = list.stream().mapToInt(Integer::intValue).sum();
//		Integer sum = list.stream().max(Integer::compare).get();
//		Integer sum2 = list.stream().min(Integer::compare).get();
//		
//		System.out.println(sum);
//		System.out.println(sum2);
//		IntStream.range(0, list.size()-1).map(i->list.get(i)+list.get(i+1)).forEach(System.out::print);
//		ArrayList<Integer> result = new ArrayList<Integer>();
		
		
		
//		list.stream().reduce((a,b)->{
//			result.add(a+b);
//			return b;
//		});
//		result.forEach(System.out::print);
	}

}
